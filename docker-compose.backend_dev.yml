version: "3.8"
services:
  backend:
    build:
      context: ./backend
      target: development
    container_name: backend-dev
    environment:
      - NODE_ENV=development
    volumes:
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules
    command: npm run dev
